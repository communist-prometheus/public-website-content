---
import type { Language } from '@/config/i18n';
import Footer from '@/features/navigation/components/Footer.astro';
import Header from '@/features/navigation/components/Header.astro';
import '@/styles/theme.css';
import '@/styles/utilities.css';

interface Props {
  title: string;
  lang: Language;
}

const { title, lang } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    <script is:inline>
      (function () {
        const theme =
          localStorage.getItem('theme') ||
          (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
        document.documentElement.dataset['theme'] = theme;
      })();
    </script>
    <script is:inline>
      (function(){var E=["click","keydown"],R=[];function r(e){var t=e.target;if(t instanceof HTMLElement&&t.closest("[defer-hydration]"))R.push({target:t,event:e})}for(var i=0;i<E.length;i++)document.addEventListener(E[i],r,{capture:true});globalThis.__cpEventReplay={replay:function(){for(var i=0;i<E.length;i++)document.removeEventListener(E[i],r,{capture:true});for(var j=0;j<R.length;j++)R[j].target.dispatchEvent(new R[j].event.constructor(R[j].event.type,R[j].event));R.length=0}}})();
    </script>
  </head>
  <body>
    <script>
      import '@/scripts/hydrate-components';
    </script>
    <Header lang={lang} />
    <main>
      <slot />
    </main>
    <Footer lang={lang} />
  </body>
</html>

<style>
  body {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }

  main {
    flex: 1;
    view-transition-name: main-content;
  }
</style>
